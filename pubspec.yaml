name: afinador
description: "Afinador Vocal"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  web: ^1.1.1
  flutter_pitch_detection: ^1.3.1
  cupertino_icons: ^1.0.9
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/og-image.png
